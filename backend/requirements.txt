# Lumiqe Backend — Pinned Python Dependencies
# Install: pip install -r requirements.txt

# Core ML
torch==2.10.0
numpy==2.2.6
scikit-learn==1.8.0

# Email validation (required by Pydantic EmailStr)
email-validator==2.3.0

# Computer Vision
opencv-python-headless==4.12.0.88
mediapipe==0.10.32

# API Server
fastapi==0.128.8
uvicorn[standard]==0.40.0
python-multipart==0.0.22

# Image Generation (palette card)
Pillow==12.1.1

# Auth (JWT + password hashing)
PyJWT==2.12.0
python-jose[cryptography]==3.5.0
bcrypt==5.0.0

# Database (PostgreSQL + pgvector)
sqlalchemy[asyncio]==2.0.46
asyncpg==0.31.0
pgvector==0.4.2
alembic==1.17.2
python-dotenv==1.2.1

# Configuration
pydantic-settings==2.12.0

# Live Scraping (Shopping Agent)
firecrawl-py==1.9.0

# LLM (Styling Tips)
groq==1.0.0

# HTTP Client
httpx==0.28.1

# Security — pinned to patched versions
aiohttp==3.13.3
werkzeug==3.1.6

# Redis async client (rate limiting + Celery broker)
redis[asyncio]==5.2.1

# Task queue (horizontal CV pipeline scaling)
celery[redis]==5.5.3

# Payments
stripe==14.4.0

# Email (Resend)
resend==2.7.0
